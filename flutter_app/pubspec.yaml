name: flutter_app
description: "Vidder — personlig platsdagbok"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.0
  mapbox_maps_flutter: ^2.18.0
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  path: ^1.9.1
  geolocator: ^14.0.2
  image_picker: ^1.2.1
  cached_network_image: ^3.4.1
  connectivity_plus: ^7.0.0
  uuid: ^4.5.3
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
